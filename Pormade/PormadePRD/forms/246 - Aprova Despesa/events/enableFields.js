function enableFields(form){ }function enableFields(form){
	
	var activity = getValue('WKNumState');
			
		/*
		if (activity == 10) {
			form.setEnabled("solicitante", false);
			form.setEnabled("gestor", false);
			form.setEnabled("datadespesa", false);
			form.setEnabled("valordespesa", false);
			
			
			var indexes = form.getChildrenIndexes("tbItens");
	    	var count = 0 ;
		    for (var i = 0; i < indexes.length; i++) {   

		    	form.setEnabled("tipodespesa___" + indexes[i], false);
		    	form.setEnabled("valordespesaitem___" + indexes[i], false);
		    	form.setEnabled("obsitem___" + indexes[i], false);

		    }  
		    
		}
		
		if (activity == 12) {
			form.setEnabled("solicitante", false);
			form.setEnabled("gestor", false);
			form.setEnabled("datadespesa", false);
			form.setEnabled("valordespesa", false);
			form.setEnabled("aprovagestor", false);
			form.setEnabled("obsaprovagestor", false);
			
			var indexes = form.getChildrenIndexes("tbItens");
	    	var count = 0 ;
		    for (var i = 0; i < indexes.length; i++) {   

		    	form.setEnabled("tipodespesa___" + indexes[i], false);
		    	form.setEnabled("valordespesaitem___" + indexes[i], false);
		    	form.setEnabled("obsitem___" + indexes[i], false);
		    	

		    }  
		    
		}
		
		if (activity == 4 ) {
			form.setEnabled("aprovafinanceiro", false);
			form.setEnabled("obsaprovafinanceiro", false);
			form.setEnabled("aprovagestor", false);
			form.setEnabled("obsaprovagestor", false);
		}
		*/
			
	}